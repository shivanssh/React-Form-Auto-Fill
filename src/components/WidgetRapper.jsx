import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const WidgetRapper = styled(Box)({
  background: "#F6F6F6",
  borderRadius: "0.25rem",
  padding: "1rem 1rem .75rem 1rem",
});

export default WidgetRapper;
